<section class="hero">
  <div class="hero__background" aria-hidden="true"></div>

  <div class="container hero__inner">
    <div class="hero__content">
      <p class="hero__eyebrow">ATATA LABS</p>

      <h1 class="hero__title">
        Websites and apps<br />
        built for your business.
      </h1>

      <p class="hero__description">
        We design and develop modern websites, web applications,
        and mobile applications for companies that value quality
        and long-term support.
      </p>

      <div class="hero__actions">
        <a class="hero__primary" href="/contact">
          Start a project
          <span aria-hidden="true">↗</span>
        </a>

        <a class="hero__secondary" href="/work">
          See our work
          <span aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    isolation: isolate;

    border-bottom: 1px solid var(--color-border);
    background: #ffffff;
  }

  /*
   * TEMPORARY TEST BACKGROUND
   *
   * This is intentionally very visible.
   * Once we confirm it renders correctly,
   * we'll turn it into the subtle final version.
   */

  .hero__background {
    position: absolute;
    z-index: 0;
    inset: 0;

    background-image:
        linear-gradient(
        rgba(34, 34, 34, 0.055) 1px,
        transparent 1px
        ),
        linear-gradient(
        90deg,
        rgba(34, 34, 34, 0.055) 1px,
        transparent 1px
        ),
        radial-gradient(
        circle at 82% 50%,
        rgba(255, 16, 83, 0.10) 0%,
        rgba(255, 16, 83, 0.04) 25%,
        transparent 55%
        );

    background-size:
        64px 64px,
        64px 64px,
        100% 100%;

    -webkit-mask-image: linear-gradient(
        to right,
        transparent 0%,
        transparent 42%,
        rgba(0, 0, 0, 0.25) 52%,
        #000 68%,
        #000 100%
    );

    mask-image: linear-gradient(
        to right,
        transparent 0%,
        transparent 42%,
        rgba(0, 0, 0, 0.25) 52%,
        #000 68%,
        #000 100%
    );

    pointer-events: none;
    }

  /*
   * Content
   */

  .hero__inner {
    position: relative;
    z-index: 1;

    min-height: 680px;

    display: flex;
    align-items: center;

    padding-top: 64px;
    padding-bottom: 96px;
  }

  .hero__content {
    max-width: 920px;
  }

  /*
   * Eyebrow
   */

  .hero__eyebrow {
    margin: 0 0 24px;

    color: var(--color-accent);

    font-size: 13px;
    font-weight: 650;
    line-height: 1;

    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  /*
   * Title
   */

  .hero__title {
    max-width: 900px;

    margin: 0 0 32px;

    color: var(--color-text);

    font-size: clamp(56px, 6.2vw, 88px);
    font-weight: 700;
    line-height: 0.98;

    letter-spacing: -0.055em;
  }

  /*
   * Description
   */

  .hero__description {
    max-width: 620px;

    margin: 0 0 40px;

    color: var(--color-text-secondary);

    font-size: 19px;
    line-height: 1.6;
  }

  /*
   * Actions
   */

  .hero__actions {
    display: flex;
    align-items: center;
    gap: 28px;
  }

  .hero__primary {
    display: inline-flex;
    align-items: center;
    gap: 10px;

    padding: 14px 20px;

    border-radius: var(--radius-sm);

    background: var(--color-accent);
    color: #ffffff;

    font-size: 15px;
    font-weight: 600;
    line-height: 1.4;

    transition:
      background var(--transition),
      transform var(--transition);
  }

  .hero__primary:hover {
    background: var(--color-accent-hover);
    transform: translateY(-1px);
  }

  .hero__secondary {
    display: inline-flex;
    align-items: center;
    gap: 8px;

    color: var(--color-text);

    font-size: 15px;
    font-weight: 600;
    line-height: 1.4;
  }

  .hero__secondary span {
    transition: transform var(--transition);
  }

  .hero__secondary:hover span {
    transform: translateX(4px);
  }

  /*
   * Tablet
   */

  @media (max-width: 768px) {
    .hero__inner {
      min-height: auto;

      padding-top: 96px;
      padding-bottom: 96px;
    }

    .hero__title {
      margin-bottom: 24px;

      font-size: clamp(44px, 12vw, 64px);
      line-height: 1;
    }

    .hero__description {
      font-size: 17px;
    }
  }

  /*
   * Mobile
   */

  @media (max-width: 520px) {
    .hero__title br {
      display: none;
    }

    .hero__actions {
      flex-direction: column;
      align-items: flex-start;
      gap: 22px;
    }
  }
</style>